{
  "name": "nahual_courses",
  "version": "0.0.0",
  "description": "An e-learning site from Nahual",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "marked": "^0.7.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.6.3",
    "nodemon": "^1.19.4"
  },
  "scripts": {
    "start": "nodemon -w src -x 'babel-node ./index.js'",
    "build": "babel src --out-dir dist",
    "serve": "node dist/index.js"
  }
}
